#include <bits/stdc++.h>

using namespace std;
int main()
{
    // vector <int> v;    //type 01!
    // cout<<v.size()<<endl;
    // vector <int > v(5);   //type 02!!
    // vector <int > v (5,10);  // type 03
    // vector <int > v2(5,100);  //type 04(copy from another vector!)
    // vector <int > v(v2);
    vector <int > v={10,20,30,40};

    for(int i=0;i<v.size();i++){
        cout<<v[i]<<endl;
    }

    return 0;
}