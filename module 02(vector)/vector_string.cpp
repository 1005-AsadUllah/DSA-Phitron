#include <bits/stdc++.h>

using namespace std;
int main()
{
    int n;
    cin>>n;
    vector<string>v(n);
    for(int i=0;i<n;i++){
        // string s;
        cin>>v[i];
        // v.push_back(s);
    }
    for(int i=0;i<n;i++){
        cout<<v[i]<<endl;
    }
    return 0;
}